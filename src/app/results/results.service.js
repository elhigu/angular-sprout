angular.module( 'app.results.service', ['app.config'])
/*
.factory( 'ResultsResource', ['$http', 'ConfService', function ($http, ConfService) {
	return {
		getRankingList : function (gameId, page) {
			$http.get(ConfService.backend + 'game/' + gameId + '/rankinglist');
		}
	};
}])
*/
;
